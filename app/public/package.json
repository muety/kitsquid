{
  "name": "kitsquid-web",
  "version": "0.0.1",
  "description": "",
  "dependencies": {
    "jquery": "^3.5.0",
    "tailwindcss": "^1.1.4"
  },
  "devDependencies": {
    "@tailwindcss/custom-forms": "^0.2.1"
  },
  "scripts": {
    "build": "npx tailwind build assets/css/app.tailwind.css -o build/css/app.css && node build.js",
    "dev": "npm run build && node build.js --watch --no-build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ferdinand Mütsch",
  "license": "MIT"
}
