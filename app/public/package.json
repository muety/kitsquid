{
  "name": "kitsquid-web",
  "version": "0.0.1",
  "description": "",
  "dependencies": {
    "jquery": "^3.5.0"
  },
  "devDependencies": {
    "@tailwindcss/custom-forms": "^0.2.1",
    "tailwindcss": "^1.9.6"
  },
  "scripts": {
    "build": "./node_modules/.bin/tailwind build assets/css/app.tailwind.css -o build/css/app.css && node build.js",
    "dev": "yarn run build && node build.js --watch --no-build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ferdinand Mütsch",
  "license": "MIT"
}
